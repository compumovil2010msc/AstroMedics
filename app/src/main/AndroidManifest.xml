<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.astromedics">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/planet"
        android:label="@string/app_name"
        android:roundIcon="@drawable/planet"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".notifications.TherapistNotificationService"
            android:exported="false"></service>

        <activity android:name=".views.therapist.ReportCreationActivity" />
        <activity android:name=".views.therapist.ReportDetails" />
        <activity android:name=".views.CreateAccount" />
        <activity android:name=".views.CreatePacientActivity" />
        <activity android:name=".views.AccountTypeSelection" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/api_key" />

        <activity android:name=".views.TherapistDetailsActivity" />
        <activity android:name=".views.HomeTherapist" />
        <activity
            android:name=".views.therapist.CreateAppointmentActivity"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.therapist.EvolutionVisualizationActivity"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.therapist.PacientDetails"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.common.AccountDetailsActivity"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.common.ChangePasswordActivity"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.pacient.ReportVisualizationActivity"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.pacient.BookAppointmentHistoryDetails"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.common.TherapistDetails"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.pacient.BookAppointmentLocationDisplayActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".views.common.BookAppointmentDetails"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.pacient.TherapistAvailableAppointmentsActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".views.therapist.HomeTherapist"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.pacient.TherapistSearchResultActivity"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.pacient.TherapistFilterActivity"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.pacient.BookAppointmentLocationSelectionActivity"
            android:theme="@style/AppThemeWithActionBar" />
        <activity
            android:name=".views.pacient.HomeUserActivity"
            android:theme="@style/AppThemeWithActionBar" />
        <activity android:name=".views.Login" />
        <activity android:name=".views.MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:noHistory="true"
            android:theme="@style/splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>